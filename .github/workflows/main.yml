on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v3
    
    - name: 📂 Sync files
      uses: Streetbuzz deploy
      with:
        server: ftp.streetbuzz.co
        username: ajay@streetbuzz.co
        password: ${{ secrets.ftp_password }}
